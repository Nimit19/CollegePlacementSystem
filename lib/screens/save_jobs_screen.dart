import 'package:flutter/material.dart';

class SavedJobScreen extends StatelessWidget {
  const SavedJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Saved Job......"),
    );
  }
}
